import 'package:flutter/cupertino.dart';

class SetingsScreen extends StatelessWidget {
  const SetingsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      'Setings Screen',
      style: TextStyle(fontSize: 40),
    );
  }
}
